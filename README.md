# Manchester Entrepreneurs Accelerate ME website

``` bash
# install node dependencies
npm install
bower install

# serve with browser sync
gulp serve

# build for production
gulp build

# deploys dist directory to ftp using secrets.json
gulp deploy
```
# accelerateme
